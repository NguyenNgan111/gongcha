import React from "react";
import style from "./OrderDetail.module.scss";
import clsx from "clsx";

const OrderDetail = () => {
  return <div className={clsx(style.container)}>OrderDetail</div>;
};

export default OrderDetail;
